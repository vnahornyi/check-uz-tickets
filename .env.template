# Environment variables template for check-uz-tickets
# Copy to .env and fill values before running the app

# Telegram bot token (required)
TELEGRAM_BOT_TOKEN=

# Database connection options (used by src/db and MikroORM)
# You can either set DATABASE_URL or the individual DB_* values.
# Example DATABASE_URL: postgres://user:pass@host:5432/dbname
DATABASE_URL=

# Individual DB connection vars (optional if DATABASE_URL is set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tickets

# Redis connection (used by worker/queue if applicable)
# If your code uses a different var, add it here.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# Optional runtime vars
NODE_ENV=development
PORT=3000

# Worker / scanning settings
# How often the periodic scanner publishes checks (in seconds). Default 180 = 3 minutes
TRACK_INTERVAL_SECONDS=180
# Hours after which notified=true is reset (default 24)
RESET_NOTIFIED_HOURS=24
# Cooldown after marking as absent (in minutes). Default 5
ABSENT_COOLDOWN_MINUTES=5

# Playwright browsers path (inside container)
# e.g. /ms-playwright when using the Playwright base image
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
